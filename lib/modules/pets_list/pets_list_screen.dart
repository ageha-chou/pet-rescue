import 'package:flutter/material.dart';

class PetsListScreen extends StatelessWidget {
  // final int id;
  //
  // PetsListScreen({required this.id});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Text('Pet List'),
      ),
    );
  }
}
